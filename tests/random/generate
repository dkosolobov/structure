#/bin/sh

rm -f list *.cnf

for vars in `seq 20 20 260`
do
    printf "$vars =" >> list
    for seed in `seq 1 5`
    do
        output=$vars.$seed.cnf
        printf " $output" >> list
        ../../bin/generate.py -k3 -s $seed $vars > $output
    done
    echo >> list
done
